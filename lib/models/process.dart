class Process {
  String id;
  bool isSelected;  
  String name;  
  double size;
  bool isDeleted;
  Process({
    this.id,
    this.isSelected,
    this.name,
    this.size,
    this.isDeleted
  });
}
